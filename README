The very basic guide to get going...

Set up the virtualenv

  # cd <path to recordwho>
  # python3 -m venv venv
  # source venv/bin/activate
  # pip install -r requirements.txt

Start Errbot:

  # errbot
